services:
  web:
    image: ghcr.io/kanbn/kan:latest
    container_name: kan-web
    ports:
      - "3300:3000"
    networks:
      - kan-network
    environment:
      NEXT_PUBLIC_BASE_URL: http://192.168.0.10:3300
      BETTER_AUTH_SECRET: base64_secret=
      POSTGRES_URL: postgresql://kan:postgres_password123@postgres:5432/kan_db
      NEXT_PUBLIC_ALLOW_CREDENTIALS: true
    depends_on:
      - postgres
    restart: unless-stopped

  postgres:
    image: postgres:15
    container_name: kan-db
    environment:
      POSTGRES_DB: kan_db
      POSTGRES_USER: kan
      POSTGRES_PASSWORD: postgres_password123
    ports:
      - 5435:5432
    volumes:
      - ./data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - kan-network

networks:
  kan-network:
